<!DOCTYPE html>
<html>
    <head>
        <title>{{title}} | {{siteName}}</title>
        {{>commonHead}}
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            <div class="body-text">
                <p>{{errorText}}</p>
            </div>
        </div>
        {{>footer}}

        {{>commonScripts}}
    </body>
</html>